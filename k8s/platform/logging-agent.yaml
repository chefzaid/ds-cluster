apiVersion: v1
kind: ServiceAccount
metadata:
  name: fluent-bit
  namespace: infra

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: fluent-bit
rules:
  - apiGroups: [""]
    resources: ["namespaces", "pods"]
    verbs: ["get", "list", "watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: fluent-bit
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: fluent-bit
subjects:
  - kind: ServiceAccount
    name: fluent-bit
    namespace: infra

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-bit-config
  namespace: infra
data:
  fluent-bit.conf: |
    [SERVICE]
        Flush                     1
        Daemon                    Off
        Log_Level                 info
        Parsers_File              /fluent-bit/etc/parsers.conf
        HTTP_Server               On
        HTTP_Listen               0.0.0.0
        HTTP_Port                 2020
        Health_Check              On
        storage.path              /var/lib/fluent-bit/storage
        storage.sync              normal
        storage.checksum          off
        storage.max_chunks_up     128

    [INPUT]
        Name                      tail
        Alias                     kubernetes_containers
        Path                      /var/log/containers/*.log
        multiline.parser          cri, docker
        Tag                       kube.*
        DB                        /var/lib/fluent-bit/tail-containers.db
        DB.Sync                   Normal
        Mem_Buf_Limit             20MB
        Skip_Long_Lines           On
        Refresh_Interval          10
        Rotate_Wait               30
        storage.type              filesystem

    [FILTER]
        Name                      kubernetes
        Match                     kube.*
        Kube_URL                  https://kubernetes.default.svc:443
        Kube_CA_File              /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
        Kube_Token_File           /var/run/secrets/kubernetes.io/serviceaccount/token
        Kube_Tag_Prefix           kube.var.log.containers.
        Merge_Log                 On
        Keep_Log                  On
        K8S-Logging.Parser        On
        K8S-Logging.Exclude       On
        Labels                    On
        Annotations               Off

    [FILTER]
        Name                      lua
        Match                     kube.*
        script                    /fluent-bit/etc/enrich-kubernetes.lua
        call                      enrich_kubernetes

    [OUTPUT]
        Name                      es
        Match                     kube.*
        Host                      elasticsearch.__INTERNAL_DNS_ZONE__
        Port                      9200
        HTTP_User                 ${ELASTICSEARCH_USERNAME}
        HTTP_Passwd               ${ELASTICSEARCH_PASSWORD}
        Logstash_Format           On
        Logstash_Prefix           kubernetes-logs-v2
        Logstash_DateFormat       %Y.%m.%d
        Suppress_Type_Name        On
        Generate_ID               On
        Write_Operation           index
        Replace_Dots              On
        Buffer_Size               5M
        Retry_Limit               False
        Trace_Error               On

  index-template.json: |
    {
      "order": 100,
      "index_patterns": ["kubernetes-logs-v2-*"],
      "settings": {
        "number_of_shards": 1,
        "number_of_replicas": 0,
        "index.refresh_interval": "5s",
        "index.mapping.total_fields.limit": 200,
        "index.mapping.ignore_malformed": true,
        "index.lifecycle.name": "bm-cluster-logs-7d"
      },
      "mappings": {
        "dynamic": false,
        "properties": {
          "@timestamp": {"type": "date"},
          "app": {"type": "keyword"},
          "observability_scope": {"type": "keyword"},
          "level": {"type": "keyword"},
          "logger_name": {"type": "keyword"},
          "thread_name": {"type": "keyword"},
          "stream": {"type": "keyword"},
          "message": {"type": "text"},
          "log": {"type": "text"},
          "timestamp": {"type": "date"},
          "context": {"type": "keyword"},
          "trace": {"type": "text"},
          "stack_trace": {"type": "text"},
          "requestId": {"type": "keyword"},
          "method": {"type": "keyword"},
          "path": {"type": "keyword"},
          "statusCode": {"type": "integer"},
          "durationMs": {"type": "float"},
          "kubernetes": {
            "dynamic": false,
            "properties": {
              "namespace_name": {"type": "keyword"},
              "pod_name": {"type": "keyword"},
              "container_name": {"type": "keyword"},
              "docker_id": {"type": "keyword"},
              "container_hash": {"type": "keyword"},
              "container_image": {"type": "keyword"},
              "host": {"type": "keyword"}
            }
          }
        }
      }
    }
  enrich-kubernetes.lua: |
    function enrich_kubernetes(tag, timestamp, record)
      local kubernetes = record["kubernetes"]
      if kubernetes == nil then
        return 0, timestamp, record
      end

      if kubernetes["namespace_name"] == "apps" then
        record["observability_scope"] = "application"
      end

      local labels = kubernetes["labels"]
      if labels ~= nil and labels["app"] ~= nil then
        record["app"] = labels["app"]
      end

      if record["message"] ~= nil and type(record["message"]) ~= "string" then
        record["message"] = nil
      end

      return 2, timestamp, record
    end
  ilm-policy.json: |
    {
      "policy": {
        "phases": {
          "hot": {
            "min_age": "0ms",
            "actions": {
              "set_priority": {"priority": 100}
            }
          },
          "delete": {
            "min_age": "7d",
            "actions": {
              "delete": {}
            }
          }
        }
      }
    }

---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: fluent-bit
  namespace: infra
  labels:
    app: fluent-bit
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  selector:
    matchLabels:
      app: fluent-bit
  template:
    metadata:
      labels:
        app: fluent-bit
      annotations:
        bm-cluster.__PUBLIC_DOMAIN__/config-version: "3"
        prometheus.io/scrape: "true"
        prometheus.io/path: /api/v2/metrics/prometheus
        prometheus.io/port: "2020"
    spec:
      serviceAccountName: fluent-bit
      automountServiceAccountToken: true
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      tolerations:
        - operator: Exists
      initContainers:
        - name: configure-index-template
          image: docker.io/curlimages/curl@sha256:58adaa4e8dca9c988bae2aba4ab3434a0bb2da16bbe3f92dec39ec7785166777
          envFrom:
            - secretRef:
                name: fluent-bit-elasticsearch-credentials
          command:
            - sh
            - -c
            - |
              until curl -fsS --user "${ELASTICSEARCH_USERNAME}:${ELASTICSEARCH_PASSWORD}" \
                http://elasticsearch.__INTERNAL_DNS_ZONE__:9200 >/dev/null; do sleep 5; done
              curl -fsS --user "${ELASTICSEARCH_USERNAME}:${ELASTICSEARCH_PASSWORD}" -X PUT \
                -H 'Content-Type: application/json' \
                --data-binary @/config/ilm-policy.json \
                http://elasticsearch.__INTERNAL_DNS_ZONE__:9200/_ilm/policy/bm-cluster-logs-7d >/dev/null
              curl -fsS --user "${ELASTICSEARCH_USERNAME}:${ELASTICSEARCH_PASSWORD}" -X PUT \
                -H 'Content-Type: application/json' \
                --data-binary @/config/index-template.json \
                http://elasticsearch.__INTERNAL_DNS_ZONE__:9200/_template/kubernetes-logs-v2 >/dev/null
              curl -fsS --user "${ELASTICSEARCH_USERNAME}:${ELASTICSEARCH_PASSWORD}" -X PUT \
                -H 'Content-Type: application/json' \
                --data-binary '{"index.lifecycle.name":"bm-cluster-logs-7d"}' \
                'http://elasticsearch.__INTERNAL_DNS_ZONE__:9200/kubernetes-logs-*,app-logs-*/_settings?allow_no_indices=true&ignore_unavailable=true' >/dev/null
          securityContext:
            runAsNonRoot: true
            runAsUser: 100
            runAsGroup: 101
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          resources:
            requests:
              cpu: 20m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
      containers:
        - name: fluent-bit
          image: cr.fluentbit.io/fluent/fluent-bit@sha256:a941bdd5ca552b2c6597fc7b3bccf2e61d30873939bab5700963de0e94ac6169
          envFrom:
            - secretRef:
                name: fluent-bit-elasticsearch-credentials
          ports:
            - name: metrics
              containerPort: 2020
          readinessProbe:
            httpGet:
              path: /api/v1/health
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/health
              port: metrics
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            runAsUser: 0
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: config
              mountPath: /fluent-bit/etc/fluent-bit.conf
              subPath: fluent-bit.conf
              readOnly: true
            - name: config
              mountPath: /fluent-bit/etc/enrich-kubernetes.lua
              subPath: enrich-kubernetes.lua
              readOnly: true
            - name: varlogcontainers
              mountPath: /var/log/containers
              readOnly: true
            - name: varlogpods
              mountPath: /var/log/pods
              readOnly: true
            - name: state
              mountPath: /var/lib/fluent-bit
      terminationGracePeriodSeconds: 30
      volumes:
        - name: config
          configMap:
            name: fluent-bit-config
        - name: varlogcontainers
          hostPath:
            path: /var/log/containers
        - name: varlogpods
          hostPath:
            path: /var/log/pods
        - name: state
          hostPath:
            path: /var/lib/fluent-bit
            type: DirectoryOrCreate

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: filebeat-lynis-config
  namespace: infra
data:
  filebeat.yml: |
    filebeat.inputs:
      - type: filestream
        id: lynis-report
        enabled: true
        paths:
          - /hostfs/var/log/lynis-report.dat
        exclude_lines: ['^[[:space:]]*(#|$)']
        prospector.scanner.check_interval: 10s
        file_identity.fingerprint: ~
        fields_under_root: true
        fields:
          event:
            dataset: lynis.audit
            module: lynis
          host:
            name: ${NODE_NAME}
          service:
            type: lynis

    output.logstash:
      hosts: ["logstash.__INTERNAL_DNS_ZONE__:5044"]
      worker: 1
      compression_level: 3
      backoff.init: 1s
      backoff.max: 60s

    setup.ilm.enabled: false
    setup.template.enabled: false

    queue.mem:
      events: 512
      flush.min_events: 1
      flush.timeout: 1s

    http.enabled: true
    http.host: 0.0.0.0
    http.port: 5066
    logging.level: info
    logging.to_files: false
    logging.metrics.enabled: false

---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: filebeat-lynis
  namespace: infra
  labels:
    app: filebeat-lynis
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    matchLabels:
      app: filebeat-lynis
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: filebeat-lynis
      annotations:
        bm-cluster.__PUBLIC_DOMAIN__/config-version: "1"
    spec:
      automountServiceAccountToken: false
      nodeSelector:
        node-role.kubernetes.io/control-plane: "true"
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
        - key: node-role.kubernetes.io/master
          operator: Exists
          effect: NoSchedule
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: filebeat
          # The standard 9.4.6 image requires x86-64-v3; Wolfi supports this node.
          image: docker.elastic.co/beats/filebeat-wolfi:9.4.6@sha256:61aec7c583a77eac1c8fdb262b5af5a9eb5945ba40e08ea381f916826323303f
          args:
            - -e
            - -c
            - /etc/filebeat/filebeat.yml
            - --strict.perms=false
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          ports:
            - name: http
              containerPort: 5066
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 192Mi
          securityContext:
            runAsUser: 0
            runAsGroup: 0
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: config
              mountPath: /etc/filebeat/filebeat.yml
              subPath: filebeat.yml
              readOnly: true
            - name: host-var-log
              mountPath: /hostfs/var/log
              readOnly: true
            - name: state
              mountPath: /usr/share/filebeat/data
      terminationGracePeriodSeconds: 30
      volumes:
        - name: config
          configMap:
            name: filebeat-lynis-config
        - name: host-var-log
          hostPath:
            path: /var/log
            type: Directory
        - name: state
          hostPath:
            path: /var/lib/bm-cluster/filebeat-lynis
            type: DirectoryOrCreate
