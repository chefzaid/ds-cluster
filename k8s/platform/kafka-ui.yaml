apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: infra
  labels:
    app: kafka-ui
spec:
  type: ClusterIP
  selector:
    app: kafka-ui
  ports:
    - name: http
      port: 8080
      targetPort: http

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-ui
  namespace: infra
  labels:
    app: kafka-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka-ui
  template:
    metadata:
      labels:
        app: kafka-ui
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
        fsGroup: 10001
      containers:
        - name: kafka-ui
          image: ghcr.io/kafbat/kafka-ui@sha256:2fc9a60b223de6d8cb45a64175afcbb79207ae19306a0b99c558aeaa1b22b6e9
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: KAFKA_CLUSTERS_0_NAME
              value: bm-cluster
            - name: KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS
              value: kafka.__INTERNAL_DNS_ZONE__:9092
            - name: KAFKA_CLUSTERS_0_READONLY
              value: "false"
            - name: AUTH_TYPE
              value: OAUTH2
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_CLIENTID
              value: kafka-ui
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_CLIENTSECRET
              valueFrom:
                secretKeyRef:
                  name: keycloak-sso-credentials
                  key: KAFKA_UI_CLIENT_SECRET
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_SCOPE
              value: openid,profile,email,groups
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_ISSUERURI
              value: https://keycloak.__PUBLIC_DOMAIN__/auth/realms/swirlit
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_USERNAMEATTRIBUTE
              value: preferred_username
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_CLIENTNAME
              value: Keycloak
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_PROVIDER
              value: keycloak
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_REDIRECTURI
              value: https://kafka.__PUBLIC_DOMAIN__/login/oauth2/code/keycloak
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_AUTHORIZATIONGRANTTYPE
              value: authorization_code
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_CUSTOMPARAMS_TYPE
              value: keycloak
            - name: AUTH_OAUTH2_CLIENT_KEYCLOAK_CUSTOMPARAMS_ROLESFIELD
              value: groups
            - name: SERVER_FORWARD_HEADERS_STRATEGY
              value: framework
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 768Mi
          volumeMounts:
          - name: tmp
            mountPath: /tmp
      volumes:
      - name: tmp
        emptyDir:
          sizeLimit: 256Mi
